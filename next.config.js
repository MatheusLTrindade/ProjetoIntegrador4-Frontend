const { checkTargetForNewValues } = require('framer-motion')


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['trocafacilstorage.blob.core.windows.net'],
  },
}

module.exports = nextConfig
